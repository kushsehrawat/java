


public class assesment{
	
	public static void main (String[] args) 
	{
		int fact = 1; 
		IBIO.output("KUSH SEHRWAT"); 
		int num = IBIO.inputInt (" Enter a integer between 1 and 12"); 
		if (num < 0) 
		{ 
			IBIO.output (" Error-number out of range");
			System.exit (1); 
  

			if (num < 0 || num > 12)
			{ 
				IBIO.output (" Error-number out of range");
				System.exit (1); 
			}                                   
			for (int i = 1; i < num+1; i++) 
			{ 
				fact = fact*i; 
			
			} 
				IBIO.output(+ num + " ! " + fact);

				IBIO.output(+ num + " ! " + fact);

			if ( num > 10) 
			IBIO.output(+ num + " ! " + fact); 
			
			if (num < 0 || num > 20) 
			{ 
				IBIO.output(" Error-number out of range");
				
			if 
			{ 
		int num = IBIO.inputInt (" Enter a integer between 1 and 12");
				for (int i = 1; i < num+1; i++) 
			{ 
				fact = fact*i; 
			
			} 
				IBIO.output(+ num + " ! " + fact);
			} 


} 


} 
